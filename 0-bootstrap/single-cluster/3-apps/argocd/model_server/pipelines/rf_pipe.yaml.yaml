apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-model-server-build-rf-pipe
  annotations:
    argocd.argoproj.io/sync-wave: "300"
  labels:
    gitops.tier.layer: applications
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: ci
    server: https://kubernetes.default.svc
  project: applications
  source:
    path: model_server/pipelines/rf_pipe
  syncPolicy:
    automated:
      prune: true
      selfHeal: true